function slider(prev_arrow, next_arrow, dots) {
    const prev = document.querySelector(prev_arrow);
    const next = document.querySelector(next_arrow);
    const dots = document.querySelectorAll(dots);


    function prevSlide() {

    }
}

export default slider;